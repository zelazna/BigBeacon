# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'big_beacon' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for big_beacon
  pod 'Alamofire', '~> 4.7'
  
  target 'big_beaconTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'big_beaconUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
